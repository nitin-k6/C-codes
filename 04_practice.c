#include<stdio.h>
int main()
 {
     int principal = 1250, rate = 4, years = 2;  
     

     int simpleInterest = (principal * rate * years )/100;
     printf ("the value of the simple interest is %d", simpleInterest);

      return 0;
 }